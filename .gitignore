/.phpunit.cache
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.*
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Local Claude Code working notes & config — never commit
# (CLAUDE.md working notes, plus all of .claude/: personal permissions,
#  overrides, and anything else containing internal hosts/paths)
/CLAUDE.md
/.claude/

# Guidearr runtime secrets — never commit
/storage/app/env-backups/
/storage/app/branding/
/certs/
docker-compose.yml

# Guidearr heartbeat runtime + secrets (never commit)
health/heartbeat.log
health/state/
health/heartbeat.env
