; Raise upload limits for branding image uploads (icon/logo).
; upload_max_filesize and post_max_size are PHP_INI_PERDIR, so PHP-FPM
; honors them from this .user.ini in the document root — no rebuild needed.
upload_max_filesize = 16M
post_max_size = 20M
memory_limit = 256M
max_execution_time = 60
